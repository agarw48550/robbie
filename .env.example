# Robbie environment overrides (optional).
# Existing ~/.config/robbie/config.json still works if these are unset.

# GOOGLE_API_KEY=
# GEMINI_API_KEY=

# ROBBIE_DEBUG=0
# ROBBIE_PI=0
# ROBBIE_BODY_AUDIO=0

# Bridge transport: http (default) | websocket
# Bridge URL still stored via `robbie set-bridge` or config.json
